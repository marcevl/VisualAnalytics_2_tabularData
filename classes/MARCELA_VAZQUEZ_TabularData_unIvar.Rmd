<br> 
<center><img src="http://i.imgur.com/sSaOozN.png" width="500"></center>

## Course: Visual Analytics for Policy and Management

### Prof. José Manuel Magallanes, PhD 

_____
<a id='part1'></a>

# Part 2: Visualizing Tabular data
### Univariate Case

_____


<span style="color:red"> Exercise:<br>Use ggplot to show a bar plot ordered by share size.</span>

```{r}
tableFreq=as.data.frame(tableFreq[order(-tableFreq$pct),]
names(tableFreq)=c("locale","pct")
#you have:
tableFreq
#Everything else remain the same

```



<span style="color:red"> Exercise:<br>Complete adding the elements missing in the last plot. </span>


```{r}
```{r nudge_gg, eval=FALSE}
lolliplot4 = lolliplot3 + geom_text(nudge_x=0.3,show.legend = FALSE) 
lolliplot4
```


```{r lolli_gg_titles, eval=FALSE}
lolliplot5 = lolliplot4 + labs(title=titleText,
                     x =NULL, 
                     y = NULL,
                     caption = sourceText)

lolliplot5
```


```{r lolli_gg_theme, eval=FALSE}
lolliplot6 = lolliplot5 + theme(
                    plot.caption = element_text(hjust = 1),
                    plot.title = element_text(hjust = 0.5))

lolliplot6
```

```



<span style="color:red"> Exercise:<br> Make sure our box plot follows the same design approach and include all the elements as in the bar plot for nominal data.
</span>
```{r}
titleTextBP='Highest grade-level offered by schools, on average'
```
```{r}
box3 = box2 + labs(title=titleTextBP,
                     x =NULL, 
                     y = NULL,
                     caption = sourceText)
box3

box4 = box3 + theme(panel.background = element_rect(fill = "white",
                                                    colour = "grey50"),
                    plot.caption = element_text(hjust = 0),
                    plot.title = element_text(hjust = 0.5))
box4
```


<span style="color:red"> Exercise:<br> Make a histogram for the variable  FREE LUNCH, and make sure it has all the right elements, and get rid of unnecessary elements.
</span>

```{r}

```{r GGplotFree.Lunch, eval=FALSE}

base= ggplot(eduwa1,aes(x = Free.Lunch))  
histogram1= base + geom_histogram(breaks=seq(0, 1780, 20), 
                          boundary=0, 
                          col="red", 
                          fill="blue", 
                          alpha=.3, 
                          na.rm = TRUE, 
                          inherit.aes = TRUE)
histogram1= histogram1 + stat_bin(binwidth = 20, aes(label=..count..), cex=1.8, 
                  geom = "text", boundary = 0,vjust=-0.5)
historam1= histogram1 + labs(title="WA Schools Providing Free Lunch", subtitle =sourceText, x="Quantity of Free Lunches", y="Number of Schools") + 
  xlim(c(0,1000))+
  ylim(c(0,360))
histogram1
```

```{r}
histogram2 = histogram1 + scale_x_continuous("Quantity of Free Lunches", breaks=seq(0, 1000, 100), limits = c(0,1000))
histogram2
```


```{r}
#base
hist(eduwa1$Free.Lunch, labels = T,xlab="Free Lunch")
```

```{r}
hist(eduwa1$Free.Lunch,labels = T, xlab = "Free Lunch", xaxt="n")
axis(side=1, at=dataHist$breaks)
```

```{r}
freq(eduwa1$Free.Lunch, style = "rmarkdown", report.nas = F)
```

```


_____
[Go to table of contents.](#part1)

[Back to course schedule menu](https://evansdatascience.github.io/VisualAnalytics/)
